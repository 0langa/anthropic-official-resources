var e=`---
kind: widget-copy
---

{/* Learner-facing copy for MarkovPhoneDemo.tsx — the "send a text"
    next-word simulator (see academy-web src/components/mdx/WidgetCopy.tsx).
    Keys are machine identifiers — translate ONLY the text between the tags.
    The reply bubble and the suggestion buttons show words sampled from the
    lesson's English training corpus (model data in the widget), so
    \`incoming\` is the only chat text held here. */}

<Copy k="incoming">hey does anyone know how to fix the build</Copy>
<Copy k="aria.suggestions">Suggested next words</Copy>
<Copy k="end">No predictions. End of chain.</Copy>
<Copy k="startOver">↻ start over</Copy>

{/* Written from this widget's copy by \`node apps/academy/scripts/describe-widgets.ts\`, run at the
    repository root. Run it again after you edit its copy. described-from: 34c56f54d30e9132 */}
<Copy k="widget.description">
  Simulation: a phone-message composer where the learner builds a reply by
  repeatedly choosing among three suggested next words, each generated by a
  first-order Markov chain trained on a small sample text. Picking a word
  appends it to the reply and the chain recalculates the top three next-word
  options based on that new last word, continuing until no prediction is
  available. The goal is to show, hands-on, how primitive keyboard-style
  autocomplete works: a model that only tracks which words have historically
  followed a given word, then samples from that narrow probability
  distribution. It sets up the lesson's broader point that this simple, fully
  interpretable table-lookup technique is the ancestor of modern next-token
  prediction in language models.
</Copy>
<Copy k="widget.summary">
  Simulation: build a text reply by repeatedly tapping one of three suggested
  next words, generated by a simple Markov chain trained on sample text. It
  demonstrates how basic keyboard autocomplete predicts words from only the
  previous word.
</Copy>
`;export{e as default};