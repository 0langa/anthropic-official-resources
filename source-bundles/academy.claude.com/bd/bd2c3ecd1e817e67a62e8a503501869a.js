var e=`---
kind: widget-copy
---

{/* Learner-facing copy for DesktopBrowserPane.tsx (see academy-web
    src/components/mdx/WidgetCopy.tsx). Keys are machine identifiers —
    translate ONLY the text between the tags. Strings drawn inside the
    picture are depicted UI and sample content and stay in the component. */}

<Copy k="aria">
  The desktop app with the built-in browser open beside the conversation:
  Claude is reading a statistics site while its message explains the step, and
  the page's chart shows in the pane
</Copy>

<Copy k="caption">
  In the desktop app the built-in browser opens beside the task. It's
  Claude's browser, not yours: it never sees your Chrome tabs, bookmarks or
  passwords, and you sign in inside it only for the sites you choose.
</Copy>
`;export{e as default};