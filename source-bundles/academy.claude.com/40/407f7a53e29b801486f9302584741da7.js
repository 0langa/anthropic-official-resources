var e=`---
kind: widget-copy
---

{/* Copy shared by the widgets in this folder (EffortByTaskShape, EffortMenu);
    merged under every widget copy file here, so the setting's name and the
    word marking its default level are translated once. */}

<Copy k="effort.label">Effort</Copy>
<Copy k="effort.default">Default</Copy>
`;export{e as default};