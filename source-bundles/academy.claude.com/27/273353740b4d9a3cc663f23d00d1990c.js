var e=`---
kind: widget-copy
---

{/* Learner-facing copy for RecordSkill.tsx (see academy-web
    src/components/mdx/WidgetCopy.tsx). Keys are machine identifiers —
    translate ONLY the text between the tags. Strings drawn inside the
    pictures are the product's own UI and stay in the component. */}

<Copy k="aria.menu">
  The plus menu under the message box, with Record a skill highlighted
</Copy>
<Copy k="aria.pill">
  The recording bar: Capturing, one step, a microphone level, Discard and Done
</Copy>

<Copy k="caption">
  Start from the plus menu, then do the task while Claude records.
</Copy>

{/* Written from this widget's copy or code by \`node apps/academy/scripts/describe-widgets.ts\`, run at the
    repository root. Run it again after you edit its copy or code. described-from: ab8cc199adcd6142 */}
<Copy k="widget.description">
  Diagram: two still frames show how to record a skill in the Claude desktop
  app. The first shows the plus menu under the message box, with Record a
  skill highlighted in a list that also holds Add files or photos, Add folder,
  Skills, Connectors, and Plugins. The second shows the small recording bar
  that appears while Claude captures the workflow, with a step count, a
  microphone level, and the choices Discard and Done. The caption gives the
  flow: start from the plus menu, then do the task while Claude records. It
  teaches that showing a task once, instead of writing instructions, can turn
  a workflow into a reusable skill.
</Copy>
<Copy k="widget.summary">
  Diagram: two still frames show recording a skill, starting from the plus
  menu under the message box, then the small recording bar that appears while
  Claude captures the task, with the choices Discard and Done.
</Copy>
`;export{e as default};