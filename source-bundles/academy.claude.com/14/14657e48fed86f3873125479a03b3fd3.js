var e=`---
kind: widget-copy
---

{/* Learner-facing copy for DesktopBrowserPane.tsx (see academy-web
    src/components/mdx/WidgetCopy.tsx). Keys are machine identifiers —
    translate ONLY the text between the tags. Strings drawn inside the
    picture are depicted UI and sample content and stay in the component. */}

<Copy k="aria">
  The desktop app with the built-in browser open beside the conversation:
  Claude is reading a statistics site while its message explains the step, and
  the page's chart shows in the pane
</Copy>

<Copy k="caption">
  In the desktop app the built-in browser opens beside the task. It's
  Claude's browser, not yours: it never sees your Chrome tabs, bookmarks or
  passwords, and you sign in inside it only for the sites you choose.
</Copy>

{/* Written from this widget's copy or code by \`node apps/academy/scripts/describe-widgets.ts\`, run at the
    repository root. Run it again after you edit its copy or code. described-from: 1fe340ecb98bc251 */}
<Copy k="widget.description">
  Illustration: a cropped view of the Claude desktop app, with a conversation
  beside the built-in browser. Claude's message says it is pulling a data
  series from a statistics website, a status line shows it reading the site,
  and the browser shows the page with its address and a line chart. It teaches
  that the built-in browser is a panel inside the desktop app where Claude
  opens sites, reads pages, and acts in them step by step. The browser is
  Claude's own, with its own tabs and history. It never sees the user's Chrome
  tabs, bookmarks, or passwords, and the user signs in inside it only for the
  sites they choose.
</Copy>
<Copy k="widget.summary">
  Illustration: the desktop app's built-in browser open beside a conversation,
  with Claude reading a statistics site. It shows that this browser is
  Claude's own, separate from yours, with its own tabs and history.
</Copy>
`;export{e as default};